# Example Sub-Agent

To create a sub-agent, add a `.md` file in `.onicode/agents/`.

The filename (without extension) becomes the agent's name.

## Format

```markdown
# Agent Name

Description of what this agent does.

## Instructions

Detailed instructions for the agent...
```

## Usage

Invoke with `@agentname` in your prompt.
