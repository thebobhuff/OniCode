# Git Commit

Generate a commit message and commit changes.

## Usage

`/git-commit`

## Prompt

Review the current git diff and generate a conventional commit message. Then stage all changes and commit.
